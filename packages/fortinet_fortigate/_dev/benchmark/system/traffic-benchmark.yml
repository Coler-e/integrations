---
description: Benchmark 150000 fortinet_fortigate traffic log events ingested end-to-end via Elastic Agent
input: logfile
data_stream:
  name: log
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/corpus-*"
    preserve_original_event: false
warmup_time_period: 10s
corpora:
  generator:
    total_events: 150000
    template:
      path: ./traffic-benchmark/gotext.tpl
      type: gotext
    config:
      path: ./traffic-benchmark/config.yml
    fields:
      path: ./traffic-benchmark/fields.yml
